exp_name: demo
model_name: resnet50
dataset: cifar10
train:
  batch_size: 64
  steps: 50
  seed: 0
  out_dir: outputs
optim:
  kind: adamw
  lr: 0.0003
  weight_decay: 0.05
sched:
  kind: cosine
  warmup_steps: 50
  min_lr: 1.0e-05
